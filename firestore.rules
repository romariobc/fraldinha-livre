rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Cada usuario le/escreve somente o proprio doc de perfil (feature 005a)
    match /users/{uid} {
      allow read, write: if request.auth != null && request.auth.uid == uid;
    }
  }
}
