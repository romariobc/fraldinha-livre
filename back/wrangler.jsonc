{
  "$schema": "node_modules/wrangler/config-schema.json",
  "name": "fraldinha-livre-backend",
  "main": "src/index.ts",
  "compatibility_date": "2026-07-19",
  "compatibility_flags": ["nodejs_compat"],
  // zod e importado por packages/contracts/src/*.ts (fora de back/), que so tem seu
  // proprio node_modules populado num install feito a partir da raiz do monorepo/workspace.
  // O build da Cloudflare instala so back/ (Diretorio raiz), entao esse alias forca a
  // resolucao pro zod ja instalado em back/node_modules, independente de onde o import parte.
  "alias": {
    "zod": "./node_modules/zod"
  },
  "observability": {
    "enabled": false,
    "head_sampling_rate": 1,
    "logs": {
      "enabled": true,
      "head_sampling_rate": 1,
      "persist": true,
      "invocation_logs": true
    }
  },
  "d1_databases": [
    {
      "binding": "DB",
      "database_name": "fraldinha-livre-db",
      // D1 real, criado via MCP cloudflare-bindings em 2026-07-19 (antecipado da B9)
      "database_id": "a6da1bcf-ed51-4c8a-8dcb-cfd0c6c9e612"
    }
  ],
  "ai": {
    "binding": "AI"
  },
  // RESEND_API_KEY e' secret (wrangler secret put RESEND_API_KEY), nunca commitado aqui.
  "vars": {
    "FIREBASE_PROJECT_ID": "fraldinha-livre",
    "NOTIFICATIONS_ENABLED": "false",
  }
}
