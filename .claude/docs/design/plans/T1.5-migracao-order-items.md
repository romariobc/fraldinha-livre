# T1.5 — Migração do Order real para items[] (núcleo seguro)

**Feature:** 016 (compra direta) · **Depende de:** T1 ✅ (OrderItem em `@/lib/domain/order`)
**Papel:** subagente SA-OrderMigration (Haiku). Coordenador revisa pelo D-012.
**Decisão:** D-018 (Order ganha items[]) — refinada aqui: fazer o NÚCLEO aditivo e sem regressão;
a reescrita dos cards/consumidores para RENDERIZAR múltiplas linhas fica no T13 (onde aparece na tela).

## ⚠️ ZONA DE RISCO — invoque a skill primeiro

Este trabalho modifica arquivos em `src/lib/` e um contexto React compartilhado. ANTES de qualquer
edição, invoque `Skill(risk-zone-protocol)` e siga o protocolo. Objetivo inegociável: **zero regressão
visual/funcional** no app atual (minha-conta e painel do fornecedor devem se comportar identicamente).

## Objetivo

Tornar o `Order` (account-mock) capaz de carregar linhas de item (`OrderItem[]`) SEM quebrar nenhum
consumidor existente. É plumbing invisível: nada na UI muda de aparência.

## Escopo — EXATAMENTE estes arquivos

### A. MODIFICAR `front/src/lib/account-mock.ts`
1. Importar o tipo: `import type { OrderItem } from '@/lib/domain/order'` (no topo).
2. Na interface `Order`, ADICIONAR o campo **opcional** canônico:
   ```ts
   items?: OrderItem[] // D-018: linhas do pedido (canonico). Opcional na transicao; getOrderItems faz o fallback.
   ```
   **NÃO remover** `product`, `quantity`, `unit`, `price` — os campos legados PERMANECEM (a UI ainda os lê).
3. Em CADA entrada de `INITIAL_ORDERS`, ADICIONAR `items` derivado dos campos existentes, com UMA linha:
   ```ts
   items: [{
     productId: `${id-do-pedido}-p1`,
     productName: <o mesmo valor de product>,
     unitPrice: <price ?? 0>,   // cotacao sem price ainda → 0
     quantity: <o mesmo quantity>,
     unit: <o mesmo unit>,
   }]
   ```
   Mantenha os campos legados intactos; só acrescente `items`. Os 5 pedidos mock devem ficar consistentes
   (items[0] espelha product/quantity/unit/price).

### B. CRIAR `front/src/lib/order-items.ts`
Helper PURO (sem React), fonte única para obter as linhas de um pedido:
```ts
import type { Order } from '@/lib/account-mock'
import type { OrderItem } from '@/lib/domain/order'

/**
 * Retorna as linhas de um pedido. Se o pedido ja tem items[] (canonico D-018), usa-o;
 * senao, deriva UMA linha dos campos legados (product/quantity/unit/price).
 */
export function getOrderItems(order: Order): OrderItem[] {
  if (order.items && order.items.length > 0) return order.items
  return [{
    productId: `${order.id}-p1`,
    productName: order.product,
    unitPrice: order.price ?? 0,
    quantity: order.quantity,
    unit: order.unit,
  }]
}
```
(Ajuste imports/tipos conforme o real; a assinatura e o comportamento acima são o contrato.)

### C. MODIFICAR `front/src/contexts/orders-context.tsx`
Nos dois criadores, popular `items` no objeto criado (mantendo os campos legados que já existem):
- `createDirectOrder(...)`: o `newOrder` ganha `items: [{ productId: \`${newOrder.id}-p1\`, productName: product, unitPrice: price, quantity, unit: 'un' }]`.
- `handleNovoPedido(...)`: o `newOrder` (cotacao) ganha `items: [{ productId: \`${newOrderId}-p1\`, productName: product, unitPrice: 0, quantity, unit: 'un' }]`.
Não altere as assinaturas das funções nem mais nada do contexto.

### D. CRIAR teste `front/src/lib/__tests__/order-items.test.ts`
Cobrir `getOrderItems`:
- Pedido COM `items[]` → retorna exatamente esse array (não deriva).
- Pedido SEM `items` (só campos legados) → deriva 1 linha correta (productId `${id}-p1`, productName=product, unitPrice=price, quantity, unit).
- Pedido sem `price` (cotacao) → unitPrice = 0.
- Use objetos `Order` montados no teste (não dependa de INITIAL_ORDERS).

## PROIBIDO neste T1.5
- NÃO tocar em: `order-adapters.ts`, `OrderCard.tsx`, `DirectOrderCard.tsx`, nenhum arquivo em
  `components/minha-conta/` ou `components/fornecedor/`, nem as páginas. Eles continuam lendo os campos
  legados — é isso que garante zero regressão. A migração deles é T13.
- NÃO remover nem tornar obrigatórios os campos legados de `Order`.
- NÃO usar `any`. NÃO usar `Date.now()`/`Math.random()` em código novo puro (o contexto já usa Date.now
  nos criadores — isso é pré-existente, NÃO refatore).

## Critério de pronto (DoD) — provar com execução

No `front/` do worktree (`E:\Labdev\Projetos\fraldinha-livre\.claude\worktrees\eloquent-montalcini-2dff41\front`):
1. `npm test` → todos verdes (92 anteriores + os novos de order-items). Colar saída real.
2. `npm run lint` → EXIT 0 (só o warning pré-existente de supplier-mock.ts).
3. `npx tsc --noEmit` → sem erro.
4. `npm run build` → **sucesso** (esta tarefa mexe em código que entra no bundle; o build precisa passar).

Loop de no máx. 3 tentativas. Se falhar depois disso, PARAR e reportar o bloqueio literal. NÃO commitar.

## Entregável
- Arquivos modificados (A, C) + criados (B, D).
- Saída literal dos 4 comandos do DoD.
- Confirmação explícita de que nenhum arquivo da lista PROIBIDA foi tocado (rode `git status --short`).
